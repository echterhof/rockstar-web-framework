# English locale file for Rockstar Web Framework
# Format: nested YAML structure that will be flattened to dot notation

error:
  authentication:
    failed: "Authentication failed"
    invalid_token: "Invalid authentication token"
    token_expired: "Authentication token has expired"
    unauthorized: "Unauthorized access"
  
  authorization:
    forbidden: "Access forbidden"
    insufficient_roles: "Insufficient roles for this action"
    insufficient_actions: "Insufficient permissions for this action"
  
  validation:
    failed: "Validation failed"
    invalid_input: "Invalid input provided"
    missing_field: "Required field '{{field}}' is missing"
    invalid_format: "Invalid format for field '{{field}}'"
    file_too_large: "File size exceeds maximum allowed size of {{max_size}} bytes"
    invalid_file_type: "File type '{{file_type}}' is not allowed"
  
  request:
    too_large: "Request size exceeds maximum allowed size"
    timeout: "Request timeout after {{timeout}} seconds"
    bogus_data: "Invalid or malformed request data"
    rate_limit_exceeded: "Rate limit exceeded: {{limit}} requests per {{window}}"
  
  security:
    csrf_invalid: "Invalid CSRF token"
    xss_detected: "Potential XSS attack detected"
    sql_injection_detected: "Potential SQL injection detected"
  
  database:
    connection: "Database connection failed"
    query: "Database query failed: {{operation}}"
    transaction: "Database transaction failed"
    record_not_found: "Record not found"
    duplicate_record: "Duplicate record"
  
  session:
    not_found: "Session not found"
    expired: "Session has expired"
    invalid: "Invalid session"
  
  configuration:
    error: "Configuration error"
    missing: "Missing required configuration: {{key}}"
  
  internal:
    server: "Internal server error"
    service_unavailable: "Service temporarily unavailable"
    not_implemented: "Feature not implemented"
  
  tenant:
    not_found: "Tenant not found"
    inactive: "Tenant is inactive"
    limit_exceeded: "Tenant limit exceeded"
  
  websocket:
    upgrade_failed: "WebSocket upgrade failed"
    connection_closed: "WebSocket connection closed"
    auth_required: "WebSocket authentication required"
    invalid_message: "Invalid WebSocket message"
  
  routing:
    not_found: "Route not found"
    method_not_allowed: "HTTP method not allowed"

log:
  server:
    starting: "Server starting on {{address}}"
    started: "Server started successfully"
    stopping: "Server stopping gracefully"
    stopped: "Server stopped"
  
  request:
    received: "Request received: {{method}} {{path}}"
    completed: "Request completed: {{method}} {{path}} - {{status}} ({{duration}}ms)"
    failed: "Request failed: {{method}} {{path}} - {{error}}"
  
  session:
    created: "Session created: {{session_id}}"
    loaded: "Session loaded: {{session_id}}"
    destroyed: "Session destroyed: {{session_id}}"
    expired: "Session expired: {{session_id}}"
  
  database:
    connected: "Database connected: {{driver}}"
    disconnected: "Database disconnected"
    query_executed: "Query executed: {{query}} ({{duration}}ms)"
  
  cache:
    hit: "Cache hit: {{key}}"
    miss: "Cache miss: {{key}}"
    set: "Cache set: {{key}}"
    deleted: "Cache deleted: {{key}}"
  
  tenant:
    registered: "Tenant registered: {{tenant_id}}"
    activated: "Tenant activated: {{tenant_id}}"
    deactivated: "Tenant deactivated: {{tenant_id}}"

message:
  welcome: "Welcome to Rockstar Web Framework"
  goodbye: "Thank you for using Rockstar Web Framework"
  
  success:
    created: "Resource created successfully"
    updated: "Resource updated successfully"
    deleted: "Resource deleted successfully"
  
  info:
    processing: "Processing request..."
    loading: "Loading data..."
    saving: "Saving data..."
